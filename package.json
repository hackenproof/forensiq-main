{
  "name": "forensiq-main",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "npx --yes prettier@3.9.6 --ignore-path .prettierignore --ignore-path .git/info/exclude --write .",
    "format:check": "npx --yes prettier@3.9.6 --ignore-path .prettierignore --ignore-path .git/info/exclude --check ."
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "validator": "^13.15.35",
    "vue": "^3.5.41",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.2.0",
    "sass-embedded": "^1.103.1"
  }
}
